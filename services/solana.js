const web3 = require("@solana/web3.js");
const config = require('../config/config').config;

async function verifyOwner(walletAddress, signature) {
    /* 
        :(
    */
    return true;
}

module.exports = {
    verifyOwner
}